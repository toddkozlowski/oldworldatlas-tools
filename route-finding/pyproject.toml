[project]
name = "route-finding"
version = "0.1.0"
description = "SVG-based pathfinding system with node-branch mapping"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "svgpathtools>=1.6.1",
    "numpy>=1.24.0",
    "lxml>=4.9.0",
]
